# Team routing decision

Return only the closed JSON object described by `team-routing-decision.schema.json`.
The candidate has already been applied to the personal store. Decide whether
the sanitized, reusable rule is suitable for the separately managed team
store. Never request or repeat prompts, responses, transcripts, paths,
credentials, client names, member names, machine identifiers, or raw event
identifiers. Preserve `public` and `private-reusable` classification only.
